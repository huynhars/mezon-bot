"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.NezonExplorerService = void 0;
require("reflect-metadata");
const common_1 = require("@nestjs/common");
const core_1 = require("@nestjs/core");
const command_decorator_1 = require("../decorators/command.decorator");
const on_decorator_1 = require("../decorators/on.decorator");
const component_decorator_1 = require("../decorators/component.decorator");
const params_decorator_1 = require("../decorators/params.decorator");
const restrict_decorator_1 = require("../decorators/restrict.decorator");
let NezonExplorerService = class NezonExplorerService {
    constructor(discoveryService, metadataScanner, reflector) {
        this.discoveryService = discoveryService;
        this.metadataScanner = metadataScanner;
        this.reflector = reflector;
    }
    exploreCommands() {
        const providers = this.discoveryService.getProviders();
        const commands = [];
        for (const wrapper of providers) {
            const { instance } = wrapper;
            if (!instance || !Object.getPrototypeOf(instance)) {
                continue;
            }
            const prototype = Object.getPrototypeOf(instance);
            this.metadataScanner.scanFromPrototype(instance, prototype, (methodName) => {
                const methodRef = prototype[methodName];
                if (!methodRef) {
                    return;
                }
                const options = this.reflector.get(command_decorator_1.NEZON_COMMAND_METADATA, methodRef);
                if (!options) {
                    return;
                }
                const classRestrict = this.reflector.get(restrict_decorator_1.NEZON_RESTRICT_METADATA, instance.constructor) ?? undefined;
                const methodRestrict = this.reflector.get(restrict_decorator_1.NEZON_RESTRICT_METADATA, methodRef) ?? undefined;
                const restricts = this.mergeRestricts(classRestrict, methodRestrict);
                const parameters = Reflect.getMetadata(params_decorator_1.NEZON_PARAMS_METADATA, prototype, methodName) ??
                    [];
                const sortedParameters = [...parameters].sort((left, right) => left.index - right.index);
                commands.push({
                    instance,
                    methodName,
                    options,
                    parameters: sortedParameters,
                    restricts,
                });
            });
        }
        return commands;
    }
    exploreEvents() {
        const providers = this.discoveryService.getProviders();
        const listeners = [];
        for (const wrapper of providers) {
            const { instance } = wrapper;
            if (!instance || !Object.getPrototypeOf(instance)) {
                continue;
            }
            const prototype = Object.getPrototypeOf(instance);
            this.metadataScanner.scanFromPrototype(instance, prototype, (methodName) => {
                const methodRef = prototype[methodName];
                if (!methodRef) {
                    return;
                }
                const event = this.reflector.get(on_decorator_1.NEZON_EVENT_METADATA, methodRef);
                if (!event) {
                    return;
                }
                const once = !!this.reflector.get(on_decorator_1.NEZON_EVENT_ONCE_METADATA, methodRef);
                const classRestrict = this.reflector.get(restrict_decorator_1.NEZON_RESTRICT_METADATA, instance.constructor) ?? undefined;
                const methodRestrict = this.reflector.get(restrict_decorator_1.NEZON_RESTRICT_METADATA, methodRef) ?? undefined;
                const restricts = this.mergeRestricts(classRestrict, methodRestrict);
                const parameters = Reflect.getMetadata(params_decorator_1.NEZON_PARAMS_METADATA, prototype, methodName) ??
                    [];
                const sortedParameters = [...parameters].sort((left, right) => left.index - right.index);
                listeners.push({
                    instance,
                    methodName,
                    event,
                    once,
                    parameters: sortedParameters,
                    restricts,
                });
            });
        }
        return listeners;
    }
    mergeRestricts(base, override) {
        if (!base && !override) {
            return undefined;
        }
        const clans = [...(base?.clans ?? []), ...(override?.clans ?? [])];
        const channels = [...(base?.channels ?? []), ...(override?.channels ?? [])];
        const users = [...(base?.users ?? []), ...(override?.users ?? [])];
        const result = {};
        if (clans.length) {
            result.clans = Array.from(new Set(clans));
        }
        if (channels.length) {
            result.channels = Array.from(new Set(channels));
        }
        if (users.length) {
            result.users = Array.from(new Set(users));
        }
        if (!result.clans?.length &&
            !result.channels?.length &&
            !result.users?.length) {
            return undefined;
        }
        return result;
    }
    exploreComponents() {
        const providers = this.discoveryService.getProviders();
        const components = [];
        for (const wrapper of providers) {
            const { instance } = wrapper;
            if (!instance || !Object.getPrototypeOf(instance)) {
                continue;
            }
            const prototype = Object.getPrototypeOf(instance);
            this.metadataScanner.scanFromPrototype(instance, prototype, (methodName) => {
                const methodRef = prototype[methodName];
                if (!methodRef) {
                    return;
                }
                const options = this.reflector.get(component_decorator_1.NEZON_COMPONENT_METADATA, methodRef) ?? null;
                if (!options) {
                    return;
                }
                const classRestrict = this.reflector.get(restrict_decorator_1.NEZON_RESTRICT_METADATA, instance.constructor) ?? undefined;
                const methodRestrict = this.reflector.get(restrict_decorator_1.NEZON_RESTRICT_METADATA, methodRef) ?? undefined;
                const restricts = this.mergeRestricts(classRestrict, methodRestrict);
                const parameters = Reflect.getMetadata(params_decorator_1.NEZON_PARAMS_METADATA, prototype, methodName) ??
                    [];
                const sortedParameters = [...parameters].sort((left, right) => left.index - right.index);
                components.push({
                    instance,
                    methodName,
                    options,
                    parameters: sortedParameters,
                    restricts,
                });
            });
        }
        return components;
    }
};
exports.NezonExplorerService = NezonExplorerService;
exports.NezonExplorerService = NezonExplorerService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [core_1.DiscoveryService,
        core_1.MetadataScanner,
        core_1.Reflector])
], NezonExplorerService);
//# sourceMappingURL=nezon-explorer.service.js.map