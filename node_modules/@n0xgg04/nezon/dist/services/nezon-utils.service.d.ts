import type { Clan } from 'mezon-sdk/dist/cjs/mezon-client/structures/Clan';
import type { TextChannel } from 'mezon-sdk/dist/cjs/mezon-client/structures/TextChannel';
import type { Message } from 'mezon-sdk/dist/cjs/mezon-client/structures/Message';
import { NezonClientService } from '../client/nezon-client.service';
import { ManagedMessage } from '../messaging/smart-message';
export declare class NezonUtilsService {
    private readonly clientService;
    private readonly logger;
    constructor(clientService: NezonClientService);
    private get client();
    getClan(id: string): Promise<Clan | undefined>;
    getChannel(id: string): Promise<TextChannel | undefined>;
    getMessage(id: string, channelId?: string): Promise<Message | undefined>;
    sendToken(recipientId: string, amount: number, note?: string): Promise<{
        ok: boolean;
        tx_hash: string;
        error: string;
    } | undefined>;
    getManagedMessage(id: string, channelId?: string): Promise<ManagedMessage | undefined>;
    private createContextFromMessage;
    private normalizeSmartMessage;
    private convertMessageContent;
    private convertAttachments;
    private convertMentions;
}
