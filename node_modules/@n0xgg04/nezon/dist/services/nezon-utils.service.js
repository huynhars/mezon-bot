"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var NezonUtilsService_1;
Object.defineProperty(exports, "__esModule", { value: true });
exports.NezonUtilsService = void 0;
const common_1 = require("@nestjs/common");
const nezon_client_service_1 = require("../client/nezon-client.service");
const smart_message_1 = require("../messaging/smart-message");
let NezonUtilsService = NezonUtilsService_1 = class NezonUtilsService {
    constructor(clientService) {
        this.clientService = clientService;
        this.logger = new common_1.Logger(NezonUtilsService_1.name);
    }
    get client() {
        return this.clientService.getClient();
    }
    async getClan(id) {
        try {
            const cached = this.client.clans?.get?.(id);
            if (cached) {
                return cached;
            }
            if (this.client.clans?.fetch) {
                return (await this.client.clans.fetch(id));
            }
        }
        catch (error) {
            this.logger.warn(`failed to fetch clan ${id}`, error?.stack);
        }
        return undefined;
    }
    async getChannel(id) {
        try {
            if (this.client.channels?.fetch) {
                const fetched = await this.client.channels.fetch(id);
                if (fetched) {
                    return fetched;
                }
            }
        }
        catch (error) {
            this.logger.warn(`failed to fetch channel ${id}`, error?.stack);
        }
        return undefined;
    }
    async getMessage(id, channelId) {
        try {
            if (channelId) {
                const channel = await this.getChannel(channelId);
                if (channel?.messages?.fetch) {
                    return (await channel.messages.fetch(id));
                }
            }
            else {
                const channels = this.client.channels?.cache;
                if (channels) {
                    for (const channel of channels.values()) {
                        try {
                            if (channel.messages?.fetch) {
                                const message = await channel.messages.fetch(id);
                                if (message) {
                                    return message;
                                }
                            }
                        }
                        catch {
                            continue;
                        }
                    }
                }
            }
        }
        catch (error) {
            this.logger.warn(`failed to fetch message ${id}`, error?.stack);
        }
        return undefined;
    }
    async sendToken(recipientId, amount, note) {
        try {
            if (this.client.sendToken) {
                return await this.client.sendToken({
                    receiver_id: recipientId,
                    amount,
                    note,
                });
            }
        }
        catch (error) {
            this.logger.warn(`failed to send token to ${recipientId}`, error?.stack);
        }
        return undefined;
    }
    async getManagedMessage(id, channelId) {
        try {
            const message = await this.getMessage(id, channelId);
            if (!message) {
                return undefined;
            }
            const channel = message.channel ||
                (channelId ? await this.getChannel(channelId) : undefined);
            if (!channel) {
                return undefined;
            }
            const channelMessage = {
                id: message.id,
                message_id: message.id,
                channel_id: channel.id,
                channel_label: '',
                clan_id: channel.clan?.id || '',
                sender_id: message.sender_id || '',
                username: '',
                avatar: '',
                content: this.convertMessageContent(message.content),
                code: 0,
                create_time: message.create_time_seconds
                    ? new Date(message.create_time_seconds * 1000).toISOString()
                    : undefined,
                update_time: undefined,
                attachments: this.convertAttachments(message.attachments),
                mentions: this.convertMentions(message.mentions),
            };
            const context = this.createContextFromMessage(channelMessage, message, channel);
            const helpers = {
                normalize: (input) => this.normalizeSmartMessage(input),
            };
            return new smart_message_1.ManagedMessage(context, helpers);
        }
        catch (error) {
            this.logger.warn(`failed to get managed message ${id}`, error?.stack);
        }
        return undefined;
    }
    createContextFromMessage(channelMessage, messageEntity, channel) {
        const cache = new Map();
        cache.set(Symbol.for('message'), messageEntity);
        cache.set(Symbol.for('channel'), channel);
        if (channel.clan) {
            cache.set(Symbol.for('clan'), channel.clan);
        }
        return {
            message: channelMessage,
            client: this.client,
            args: [],
            cache,
            reply: async (...replyArgs) => {
                if (typeof messageEntity.reply === 'function') {
                    return messageEntity.reply(...replyArgs);
                }
                return undefined;
            },
            getChannel: async () => channel,
            getClan: async () => channel.clan || undefined,
            getUser: async () => {
                if (!channelMessage.sender_id) {
                    return undefined;
                }
                try {
                    const client = this.client;
                    if (client.users?.fetch) {
                        return (await client.users.fetch(channelMessage.sender_id));
                    }
                }
                catch {
                    return undefined;
                }
                return undefined;
            },
            getMessage: async () => messageEntity,
            getMessageByIds: async (msgChannelId, msgId) => {
                try {
                    const msgChannel = await this.getChannel(msgChannelId);
                    if (msgChannel?.messages?.fetch) {
                        return (await msgChannel.messages.fetch(msgId));
                    }
                }
                catch {
                    return undefined;
                }
                return undefined;
            },
        };
    }
    normalizeSmartMessage(input) {
        if (input instanceof smart_message_1.SmartMessage) {
            return input.toJSON();
        }
        if (typeof input === 'string') {
            return { content: { t: input } };
        }
        if (input &&
            typeof input === 'object' &&
            'content' in input &&
            typeof input.content === 'object') {
            const normalized = input;
            return {
                content: { ...normalized.content },
                attachments: normalized.attachments?.map((attachment) => ({
                    ...attachment,
                })),
                mentions: normalized.mentions?.map((mention) => ({ ...mention })),
                mentionPlaceholders: (0, smart_message_1.cloneMentionPlaceholders)(normalized.mentionPlaceholders),
            };
        }
        if (input && typeof input === 'object') {
            return { content: input };
        }
        return { content: { t: String(input ?? '') } };
    }
    convertMessageContent(content) {
        if (typeof content === 'string') {
            return content;
        }
        if (content &&
            typeof content === 'object' &&
            't' in content &&
            typeof content.t === 'string') {
            return content.t;
        }
        return '';
    }
    convertAttachments(attachments) {
        if (Array.isArray(attachments)) {
            return attachments;
        }
        return undefined;
    }
    convertMentions(mentions) {
        if (Array.isArray(mentions)) {
            return mentions.map((m) => {
                if (typeof m === 'object' && m !== null && 'user_id' in m) {
                    return { user_id: String(m.user_id) };
                }
                return { user_id: String(m) };
            });
        }
        return undefined;
    }
};
exports.NezonUtilsService = NezonUtilsService;
exports.NezonUtilsService = NezonUtilsService = NezonUtilsService_1 = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [nezon_client_service_1.NezonClientService])
], NezonUtilsService);
//# sourceMappingURL=nezon-utils.service.js.map