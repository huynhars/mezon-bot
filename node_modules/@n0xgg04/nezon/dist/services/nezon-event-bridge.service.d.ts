import { OnApplicationBootstrap, OnApplicationShutdown } from '@nestjs/common';
import { EventEmitter2 } from '@nestjs/event-emitter';
import { NezonClientService } from '../client/nezon-client.service';
import type { NezonModuleOptions } from '../nezon.module-interface';
export declare class NezonEventBridgeService implements OnApplicationBootstrap, OnApplicationShutdown {
    private readonly clientService;
    private readonly eventEmitter;
    private readonly options;
    private readonly logger;
    private readonly unsubscribers;
    constructor(clientService: NezonClientService, eventEmitter: EventEmitter2, options: NezonModuleOptions);
    onApplicationBootstrap(): Promise<void>;
    onApplicationShutdown(): void;
    private bindChannelMessage;
    private bindAddClanUser;
    private bindSimpleEvent;
    private registerListener;
}
