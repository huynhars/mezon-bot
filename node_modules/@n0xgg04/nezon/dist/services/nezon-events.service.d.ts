import { EventEmitter2 } from '@nestjs/event-emitter';
import { NezonClientService } from '../client/nezon-client.service';
import { NezonExplorerService } from './nezon-explorer.service';
import type { NezonModuleOptions } from '../nezon.module-interface';
import { ModuleRef, Reflector } from '@nestjs/core';
export declare class NezonEventsService {
    private readonly explorer;
    private readonly clientService;
    private readonly eventEmitter;
    private readonly moduleOptions;
    private readonly moduleRef;
    private readonly reflector;
    private readonly logger;
    private handlers;
    private isInitialized;
    constructor(explorer: NezonExplorerService, clientService: NezonClientService, eventEmitter: EventEmitter2, moduleOptions: NezonModuleOptions, moduleRef: ModuleRef, reflector: Reflector);
    initialize(): void;
    dispose(): void;
    private bind;
    private executeEvent;
    private canActivateGuards;
    private getGuardInstance;
    private isAllowedForPayload;
    private mergeRestricts;
    private resolveEventArguments;
    private extractMessageContent;
    private getChannelFromEvent;
    private getClanFromEvent;
    private getUserFromEvent;
    private getMessageEntityFromEvent;
    private fetchMessageByIds;
    private normalizeSmartMessage;
}
