"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Nezon = void 0;
const mezon_sdk_1 = require("mezon-sdk");
const Messaging = require("../messaging/smart-message");
const ButtonModule = require("../messaging/button-builder");
const EmbedModule = require("../messaging/embed-builder");
/**
 * Convenience namespace that re-exports the most common mezon-sdk types and
 * Nezon-specific helpers. Consumers can import `Nezon` alongside the decorators
 * to keep type hints consistent:
 *
 * ```ts
 * import { Command, AutoContext } from '@n0xgg04/nezon';
 * import type { Nezon } from '@n0xgg04/nezon';
 *
 * @Command('ping')
 * async onPing(@AutoContext() [message]: Nezon.AutoContext) {
 *   await message.reply(Nezon.SmartMessage.text('pong'));
 * }
 * ```
 */
var Nezon;
(function (Nezon) {
    Nezon.SmartMessage = Messaging.SmartMessage;
    Nezon.ButtonBuilder = ButtonModule.ButtonBuilder;
    Nezon.ButtonStyle = ButtonModule.ButtonStyle;
    Nezon.EmbedBuilder = EmbedModule.EmbedBuilder;
    Nezon.Events = mezon_sdk_1.Events;
})(Nezon || (exports.Nezon = Nezon = {}));
//# sourceMappingURL=index.js.map