"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.MessageDatabase = void 0;
const better_sqlite3_1 = __importDefault(require("better-sqlite3"));
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
class MessageDatabase {
    constructor(dbPath = "./mezon-cache/mezon-messages-cache.db") {
        const dir = path_1.default.dirname(dbPath);
        if (!fs_1.default.existsSync(dir)) {
            fs_1.default.mkdirSync(dir, { recursive: true });
        }
        this.ensureGitIgnore(dbPath);
        this.db = new better_sqlite3_1.default(dbPath);
        this.init();
    }
    ensureGitIgnore(dbPath) {
        const gitignorePath = path_1.default.resolve(".gitignore");
        const dirName = path_1.default.dirname(dbPath);
        let relativeDir = dirName.replace(/\\/g, "/");
        if (relativeDir.startsWith("./")) {
            relativeDir = relativeDir.slice(2);
        }
        try {
            let gitignoreContent = "";
            if (fs_1.default.existsSync(gitignorePath)) {
                gitignoreContent = fs_1.default.readFileSync(gitignorePath, "utf-8");
            }
            if (!gitignoreContent.includes(relativeDir)) {
                fs_1.default.appendFileSync(gitignorePath, `\n# Mezon cache\n/${relativeDir}`);
                console.log(`[mezon] Added "${relativeDir}" to .gitignore`);
            }
        }
        catch (err) {
            console.warn("[mezon] Could not modify .gitignore:", err);
        }
    }
    init() {
        this.db
            .prepare(`CREATE TABLE IF NOT EXISTS messages_v2 (
          id TEXT NOT NULL,
          channel_id TEXT NOT NULL,
          clan_id TEXT NOT NULL,
          sender_id TEXT,
          content TEXT,
          mentions TEXT,
          attachments TEXT,
          reactions TEXT,
          msg_references TEXT,
          topic_id TEXT,
          create_time_seconds INTEGER,
          PRIMARY KEY (id, channel_id, clan_id)
        )`)
            .run();
        this.db
            .prepare(`CREATE INDEX IF NOT EXISTS idx_messages_v2_channel 
         ON messages_v2(channel_id)`)
            .run();
    }
    saveMessage(message) {
        var _a, _b, _c, _d, _e, _f, _g, _h, _j;
        const stmt = this.db.prepare(`INSERT OR REPLACE INTO messages_v2 (
        id, channel_id, clan_id, sender_id,
        content, mentions, attachments, reactions,
        msg_references, topic_id, create_time_seconds
      ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`);
        stmt.run(message.message_id, message.channel_id, (_a = message.clan_id) !== null && _a !== void 0 ? _a : "", (_b = message.sender_id) !== null && _b !== void 0 ? _b : "", JSON.stringify((_c = message.content) !== null && _c !== void 0 ? _c : {}), JSON.stringify((_d = message.mentions) !== null && _d !== void 0 ? _d : []), JSON.stringify((_e = message.attachments) !== null && _e !== void 0 ? _e : []), JSON.stringify((_f = message.reactions) !== null && _f !== void 0 ? _f : []), JSON.stringify((_g = message.references) !== null && _g !== void 0 ? _g : []), (_h = message.topic_id) !== null && _h !== void 0 ? _h : null, (_j = message.create_time_seconds) !== null && _j !== void 0 ? _j : null);
    }
    getMessageById(messageId, channelId, clanId) {
        const stmt = this.db.prepare(`SELECT * FROM messages_v2 
       WHERE id = ? AND channel_id = ? AND clan_id = ?
       LIMIT 1`);
        const row = stmt.get(messageId, channelId, clanId);
        if (!row)
            return null;
        return Object.assign(Object.assign({}, row), { content: JSON.parse(row.content), mentions: JSON.parse(row.mentions), attachments: JSON.parse(row.attachments), reactions: JSON.parse(row.reactions), references: JSON.parse(row.msg_references) });
    }
}
exports.MessageDatabase = MessageDatabase;
//# sourceMappingURL=MessageDatabase.js.map