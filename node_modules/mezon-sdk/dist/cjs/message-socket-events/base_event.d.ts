import { Socket } from "../interfaces";
export declare abstract class BaseSocketEvent {
    protected socket: Socket;
    abstract handleFunctions: ((...args: any[]) => void)[];
    abstract event: string;
    constructor(socket: Socket);
    excute(): void;
}
