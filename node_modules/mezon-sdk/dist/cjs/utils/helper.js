"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.waitFor2nTimeout = exports.generateSnowflakeId = exports.parseUrlToHostAndSSL = exports.sleep = exports.isValidUserId = exports.convertChanneltypeToChannelMode = exports.convertInternalEventToEvents = void 0;
const constants_1 = require("../constants");
let sequence = BigInt("0");
let lastTimestamp = BigInt("0");
function convertInternalEventToEvents(input) {
    return input.replace(/_event/g, "").replace(/_/g, "");
}
exports.convertInternalEventToEvents = convertInternalEventToEvents;
function convertChanneltypeToChannelMode(channelType) {
    switch (Number(channelType)) {
        case constants_1.ChannelType.CHANNEL_TYPE_DM:
            return constants_1.ChannelStreamMode.STREAM_MODE_DM;
        case constants_1.ChannelType.CHANNEL_TYPE_GROUP:
            return constants_1.ChannelStreamMode.STREAM_MODE_GROUP;
        case constants_1.ChannelType.CHANNEL_TYPE_CHANNEL:
        case constants_1.ChannelType.CHANNEL_TYPE_APP:
        case constants_1.ChannelType.CHANNEL_TYPE_MEZON_VOICE:
            return constants_1.ChannelStreamMode.STREAM_MODE_CHANNEL;
        case constants_1.ChannelType.CHANNEL_TYPE_THREAD:
            return constants_1.ChannelStreamMode.STREAM_MODE_THREAD;
    }
    return 0;
}
exports.convertChanneltypeToChannelMode = convertChanneltypeToChannelMode;
function isValidUserId(userId) {
    if (typeof userId === "string" || typeof userId === "number") {
        const strId = userId.toString();
        return /^\d+$/.test(strId);
    }
    return false;
}
exports.isValidUserId = isValidUserId;
function sleep(ms) {
    return new Promise((resolve) => setTimeout(resolve, ms));
}
exports.sleep = sleep;
function parseUrlToHostAndSSL(urlStr) {
    const url = new URL(urlStr);
    return {
        host: url.hostname,
        port: url.port || (url.protocol === "https:" ? "443" : "80"),
        useSSL: url.protocol === "https:",
    };
}
exports.parseUrlToHostAndSSL = parseUrlToHostAndSSL;
function generateSnowflakeId() {
    const epoch = BigInt("1577836800000");
    const timestamp = BigInt(Date.now().toString());
    if (timestamp === lastTimestamp) {
        sequence = sequence + BigInt("1");
    }
    else {
        sequence = BigInt("0");
        lastTimestamp = timestamp;
    }
    const workerId = BigInt("1");
    const dataCenterId = BigInt("1");
    const snowflakeId = ((timestamp - epoch) << BigInt("22")) |
        (dataCenterId << BigInt("17")) |
        (workerId << BigInt("12")) |
        sequence;
    return snowflakeId.toString();
}
exports.generateSnowflakeId = generateSnowflakeId;
async function waitFor2nTimeout(action, maxAttempts = 10) {
    let attempt = 1;
    let delay = 5000;
    const maxDelay = 60000;
    while (attempt <= maxAttempts) {
        try {
            if (attempt > 1) {
                await sleep(delay);
            }
            const result = await action();
            console.log("Action successful!");
            return result;
        }
        catch (error) {
            if (attempt >= maxAttempts) {
                console.error("Max attempts reached.");
                throw error;
            }
            console.warn(`Attempt ${attempt} failed at ${new Date().toLocaleString()}: ${JSON.stringify(error !== null && error !== void 0 ? error : {})}\nAttempt ${attempt + 1} is running...`);
            delay = Math.min(delay * 2, maxDelay);
            attempt++;
        }
    }
    throw new Error("Unreachable");
}
exports.waitFor2nTimeout = waitFor2nTimeout;
//# sourceMappingURL=helper.js.map