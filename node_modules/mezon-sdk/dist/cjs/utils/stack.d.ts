import { Client, MessagePayLoad, MessageUserPayLoad } from "../interfaces";
export declare class Stack {
    private messageProcessPerTime;
    private interval;
    private client;
    private callback;
    private isToUser;
    private items;
    private isProcessing;
    private intervalId;
    constructor(messageProcessPerTime: number, interval: number, client: Client, callback: Function, isToUser?: boolean);
    push(element: MessagePayLoad | MessagePayLoad[] | MessageUserPayLoad | MessageUserPayLoad[]): void;
    private processQueue;
}
