/// <reference types="node" />
import { EventEmitter } from "events";
import { CacheManager } from "../utils/CacheManager";
import { Clan } from "../structures/Clan";
import { TextChannel } from "../structures/TextChannel";
import { MezonApi } from "../../api";
import { SocketManager } from "../manager/socket_manager";
import { SessionManager } from "../manager/session_manager";
import { EventManager } from "../manager/event_manager";
import { ApiGetZkProofRequest, ApiQuickMenuAccessPayload, APISentTokenRequest, ChannelMessage, ClientConfigDto } from "../../interfaces";
import { ChannelManager } from "../manager/channel_manager";
import { User } from "../structures/User";
import { AsyncThrottleQueue } from "../utils/AsyncThrottleQueue";
import { Session } from "../../session";
import { MessageDatabase } from "../../sqlite/MessageDatabase";
import { IEphemeralKeyPair, IZkProof, MmnClient, ZkClient } from "mmn-client-js";
export declare class MezonClientCore extends EventEmitter {
    token: string;
    clientId: string;
    host: string;
    useSSL: boolean;
    port: string;
    protected readonly timeout: number;
    loginBasePath: string | undefined;
    mmnApiUrl: string | undefined;
    zkApiUrl: string | undefined;
    keyGen: IEphemeralKeyPair;
    addressMMN: string;
    zkProofs: IZkProof;
    private _mmnInitPromise?;
    protected apiClient: MezonApi;
    protected _mmnClient: MmnClient;
    protected _zkClient: ZkClient;
    protected socketManager: SocketManager;
    protected channelManager: ChannelManager;
    protected sessionManager: SessionManager;
    protected eventManager: EventManager;
    protected messageQueue: AsyncThrottleQueue;
    clans: CacheManager<string, Clan>;
    channels: CacheManager<string, TextChannel>;
    users: CacheManager<string, User>;
    protected messageDB: MessageDatabase;
    constructor(config: ClientConfigDto);
    get mmnClient(): MmnClient;
    get zkClient(): ZkClient;
    initManager(basePath: string, sessionApi?: Session): void;
    handleClientLogin(): Promise<string>;
    login(): Promise<string>;
    getEphemeralKeyPair(): Promise<IEphemeralKeyPair>;
    getAddress(user_id: string): Promise<string>;
    getZkProofs(data: ApiGetZkProofRequest): Promise<IZkProof>;
    getCurrentNonce(user_id: string, tag?: "latest" | "pending"): Promise<import("mmn-client-js").GetCurrentNonceResponse>;
    private mmnInitialized;
    sendToken(tokenEvent: APISentTokenRequest): Promise<import("mmn-client-js").AddTxResponse>;
    addQuickMenuAccess(body: ApiQuickMenuAccessPayload): Promise<any>;
    deleteQuickMenuAccess(botId?: string): Promise<any>;
    closeSocket(): void;
    getListFriends(limit?: number, state?: string, cursor?: string): Promise<any>;
    acceptFriend(userId: string, username: string): Promise<any>;
    addFriend(username: string): Promise<any>;
    listTransactionDetail(transactionId: string): Promise<any>;
    protected _fetchClanFromAPI(id: string): Promise<Clan>;
    protected _fetchChannelFromAPI(id: string): Promise<TextChannel>;
    protected _fetchUserFromAPI(id: string): Promise<User>;
    protected _initChannelMessageCache(e: ChannelMessage): Promise<void>;
    protected _initUserClanCache(e: ChannelMessage): Promise<void>;
    protected _updateCacheChannel(e: any): void;
}
