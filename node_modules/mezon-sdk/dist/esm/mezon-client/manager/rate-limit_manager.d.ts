type AsyncFn<T> = () => Promise<T>;
export declare class RateLimiter {
    readonly minTimeMs: number;
    private queue;
    private running;
    private lastRunAt;
    constructor(minTimeMs?: number);
    schedule<T>(fn: AsyncFn<T>): Promise<T>;
    private run;
}
export {};
