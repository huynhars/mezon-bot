import { ErrorEvent, CloseEvent } from "ws";
import { MezonApi } from "../../api";
import { WebSocketAdapter } from "../../web_socket_adapter";
import { Socket } from "../../interfaces/socket";
import { Session } from "../../session";
import { EphemeralMessageData, ReactMessageData, RemoveMessageData, ReplyMessageData, UpdateMessageData } from "../../interfaces";
import { AsyncThrottleQueue } from "../utils/AsyncThrottleQueue";
import { MessageDatabase } from "../../sqlite/MessageDatabase";
import { MezonClientCore } from "../client/MezonClientCore";
export declare class SocketManager {
    private host;
    private port;
    private useSSL;
    private adapter;
    private apiClient;
    private messageQueue;
    private client;
    private messageDB;
    [key: string]: any;
    private socket;
    private isHardDisconnect;
    private isRetrying;
    constructor(host: string, port: string, useSSL: boolean, adapter: WebSocketAdapter, apiClient: MezonApi, messageQueue: AsyncThrottleQueue, client: MezonClientCore, messageDB: MessageDatabase);
    createSocket(): void;
    getSocket(): Socket;
    connect(sockSession: Session): Promise<Session>;
    closeSocket(): void;
    isOpen(): boolean;
    onerror(evt: ErrorEvent): Promise<void>;
    onheartbeattimeout(): void;
    ondisconnect(e: CloseEvent): void;
    connectSocket(sessionToken: string): Promise<void>;
    retriesConnect(): Promise<void>;
    writeChatMessage(dataWriteMessage: ReplyMessageData): Promise<import("../../interfaces").ChannelMessageAck>;
    writeEphemeralMessage(dataWriteMessage: EphemeralMessageData): Promise<import("../../interfaces").ChannelMessageAck>;
    updateChatMessage(dataUpdateMessage: UpdateMessageData): Promise<import("../../interfaces").ChannelMessageAck>;
    writeMessageReaction(dataReactionMessage: ReactMessageData): Promise<import("../../interfaces").ApiMessageReaction>;
    removeChatMessage(dataRemoveMessage: RemoveMessageData): Promise<import("../../interfaces").ChannelMessageAck>;
}
