import { ApiMessageAttachment, ChannelMessageContent } from "../../interfaces";
import { ChannelManager } from "../manager/channel_manager";
import { SocketManager } from "../manager/socket_manager";
import { AsyncThrottleQueue } from "../utils/AsyncThrottleQueue";
export interface UserInitData {
    id: string;
    username?: string;
    clan_nick?: string;
    clan_avatar?: string;
    display_name?: string;
    avartar?: string;
    dmChannelId?: string;
}
type UserDeps = {
    socketManager: SocketManager;
    messageQueue: AsyncThrottleQueue;
    channelManager: ChannelManager;
};
export declare class User {
    id: string;
    username: string;
    clan_nick: string;
    clan_avatar: string;
    display_name: string;
    avartar: string;
    dmChannelId: string;
    private readonly socketManager;
    private readonly messageQueue;
    private readonly channelManager;
    constructor(initUserData: UserInitData, deps: UserDeps);
    private createDmChannel;
    sendDM(content: ChannelMessageContent, code?: number, attachments?: Array<ApiMessageAttachment>): Promise<import("../../interfaces").ChannelMessageAck>;
}
export {};
