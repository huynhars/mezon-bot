import { ChannelType, InternalEventsSocket } from "../constants";
import { BaseSocketEvent } from "./base_event";
export class UserChannelUpdated extends BaseSocketEvent {
    handleFunctions = [this.joinChannel];
    event = InternalEventsSocket.ChannelUpdatedEvent;
    async joinChannel(input) {
        if (input.channel_type === ChannelType.CHANNEL_TYPE_THREAD &&
            input.status === 1)
            await this.socket.joinChat(input.clan_id, input.channel_id, input.channel_type, false);
    }
}
//# sourceMappingURL=user_channel_updated.js.map